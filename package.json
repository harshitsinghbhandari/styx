{
  "name": "styx",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "kernel",
    "router",
    "runner",
    "agent",
    "ui"
  ],
  "scripts": {
    "test:kernel": "npm run test --workspace=kernel"
  }
}
